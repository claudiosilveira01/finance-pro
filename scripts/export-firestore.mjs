// Placeholder — implementado no Passo 4 da migração (docs/ESTADO-MIGRACAO.md).
// Vai usar firebase-admin + scripts/serviceAccountKey.json (gitignored) para exportar
// users/*/config/* e users/*/meses/* para firestore-export.json, junto com o mapa
// uid -> e-mail (Admin Auth listUsers) para a recriação de contas no Supabase Auth.
